{
  "name": "@cookie-dev/cookie-manager",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "cookie-manager": "bin/run.js"
  },
  "scripts": {
    "dev": "tsx src/cli.ts --help",
    "build": "tsc -p tsconfig.json",
    "types:check": "tsc -p tsconfig.json --noEmit",
    "lint": "oxlint src",
    "test": "vitest run"
  },
  "dependencies": {
    "diff": "^7.0.0",
    "marked": "^15.0.12",
    "marked-terminal": "^7.3.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/diff": "^7.0.2",
    "vitest": "^3.2.4"
  }
}
