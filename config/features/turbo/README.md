# Turbo

This feature defines the pnpm workspace, Turborepo config, and CI workflow.

## Required environment

- `TURBO_TOKEN` (GitHub Actions secret): Used for Turborepo remote caching in CI.
- `TURBO_TEAM` (GitHub Actions variable): Team slug for the Turborepo remote cache.
